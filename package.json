{
  "name": "canvas-remote-play",
  "private": true,
  "type": "module",
  "scripts": {
    "build:decoder": "node scripts/build-decoder.mjs",
    "test": "node --test tests/input.test.mjs && playwright test",
    "test:unit": "node --test tests/input.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2"
  }
}
